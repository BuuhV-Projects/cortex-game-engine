[**cortex-game-engine**](../README.md)

***

[cortex-game-engine](../README.md) / pixelate

# Function: pixelate()

> **pixelate**(`texture`): `Texture`

Defined in: [.claude/worktrees/feat-input-rebind/src/scene/Sprite.ts:16](https://github.com/BuuhV-Projects/cortex-game-engine/blob/main/src/scene/Sprite.ts#L16)

Configura a textura pra **pixel art**: amostragem **nearest** (sem borrar ao
escalar) e sem mipmaps. Retorna a própria textura (encadeável).

## Parameters

### texture

`Texture`

## Returns

`Texture`
